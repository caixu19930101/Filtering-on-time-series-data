# Filtering-on-time-series-data
Using python to filterion time-series data£¬
outputs a plot of the filtered and unfiltered data¡£
I use IDE is jupyter, so the file should be .ipynb, but i transfer to .py.

